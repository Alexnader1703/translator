int x, y;
begin
    x = 5;
    y = 10;
    if(x < y) then
        x = x + 1;
    elseif(x == y) then
        y = y - 1;
    else
        x = x - 1;
    endif;
    while(x < y)
        x = x + 1;
    endwhile;
end

